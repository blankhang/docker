version: '3.7'

services:

  redis:
    image: redis:6
    restart: always
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - "./data:/data"
    #command: redis-server --appendonly yes --requirepass "blank@redis"
    environment:
      - TZ=Asia/Shanghai