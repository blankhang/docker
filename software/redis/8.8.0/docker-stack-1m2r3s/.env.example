# 本 stack 按区域各有一份完整 compose：
#   ./scripts/deploy.sh sz   → redis-stack-sz.yml（5 节点）
#   ./scripts/deploy.sh wh   → redis-stack-wh.yml（3 节点）
#
# 验收脚本密码（与 Swarm secret redis_1m2r3s_password 一致）：
export REDIS_PASSWORD=changeme
./scripts/check-replication.sh sz   # 或 wh
