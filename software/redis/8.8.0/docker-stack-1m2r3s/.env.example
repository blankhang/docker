# 本 stack 按区域部署，勿在此写死机房 IP：
#   cp env/sz.env env/local.env   # 可选
#   ./scripts/deploy.sh sz
#   ./scripts/deploy.sh wh
#
# 验收脚本密码（与 Swarm secret 一致）：
export REDIS_PASSWORD=changeme
./scripts/check-replication.sh sz   # 或 wh
