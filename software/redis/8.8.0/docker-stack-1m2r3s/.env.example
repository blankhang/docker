# 复制后用于 check-replication.sh 等脚本:
#   cp .env.example .env && set -a && source .env && set +a
#
# Swarm 部署时密码写入 Secret（勿写入 compose 文件）:
#   printf '%s' "$REDIS_PASSWORD" | docker secret create redis_1m2r3s_password -

REDIS_PASSWORD=changeme
