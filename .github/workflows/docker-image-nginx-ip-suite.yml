name: Docker Image Nginx IP Suite CI

on:
  workflow_dispatch:
    inputs:
      nginx_version:
        description: "指定 nginx 版本，留空则自动检测最新稳定版"
        required: false
        type: string
      force_build:
        description: "即使镜像标签已存在也强制重新构建"
        required: false
        default: false
        type: boolean
      set_latest:
        description: "手动构建时，如果版本正好是当前最新稳定版则同步更新 latest"
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: docker-image-nginx-ip-suite
  cancel-in-progress: false

jobs:
  detect-version:
    runs-on: ubuntu-latest
    outputs:
      target_version: ${{ steps.detect.outputs.target_version }}
      set_latest: ${{ steps.detect.outputs.set_latest }}
    steps:
      - name: Detect target version for full suite
        id: detect
        env:
          INPUT_NGINX_VERSION: ${{ github.event.inputs.nginx_version || '' }}
          MANUAL_SET_LATEST: ${{ github.event.inputs.set_latest || 'false' }}
        run: |
          python3 <<'PY'
          import json
          import os
          import re
          import urllib.request

          stable_re = re.compile(r"^\d+\.\d+\.\d+$")

          def parse_version(version):
              return tuple(map(int, version.split(".")))

          def is_stable_release(version):
              _, minor, _ = parse_version(version)
              return minor % 2 == 0

          def fetch_json(url):
              req = urllib.request.Request(
                  url,
                  headers={"User-Agent": "github-actions-nginx-ip-suite"},
              )
              with urllib.request.urlopen(req, timeout=30) as resp:
                  return json.load(resp)

          def latest_stable_nginx():
              url = "https://hub.docker.com/v2/namespaces/library/repositories/nginx/tags?page_size=100"
              versions = set()
              while url:
                  payload = fetch_json(url)
                  for item in payload.get("results", []):
                      name = item.get("name", "")
                      if stable_re.fullmatch(name) and is_stable_release(name):
                          versions.add(name)
                  url = payload.get("next")
              if not versions:
                  raise RuntimeError("未获取到 nginx 稳定版标签")
              return max(versions, key=parse_version)

          requested_version = os.environ.get("INPUT_NGINX_VERSION", "").strip()
          manual_set_latest = os.environ.get("MANUAL_SET_LATEST", "false").lower() == "true"
          latest_version = latest_stable_nginx()
          target_version = requested_version or latest_version
          set_latest = (target_version == latest_version) and (
              requested_version == "" or manual_set_latest
          )

          output_path = os.environ["GITHUB_OUTPUT"]
          with open(output_path, "a", encoding="utf-8") as fh:
              fh.write(f"target_version={target_version}\n")
              fh.write(f"set_latest={'true' if set_latest else 'false'}\n")
          PY

      - name: Show suite decision
        run: |
          echo "target_version=${{ steps.detect.outputs.target_version }}"
          echo "set_latest=${{ steps.detect.outputs.set_latest }}"

  ip2region:
    needs: detect-version
    uses: ./.github/workflows/docker-image-nginx-ip2region.yml
    with:
      nginx_version: ${{ needs.detect-version.outputs.target_version }}
      force_build: ${{ github.event.inputs.force_build == 'true' }}
      set_latest: ${{ needs.detect-version.outputs.set_latest == 'true' }}
    secrets: inherit

  geoip2:
    needs: detect-version
    uses: ./.github/workflows/docker-image-nginx-geoip2.yml
    with:
      nginx_version: ${{ needs.detect-version.outputs.target_version }}
      force_build: ${{ github.event.inputs.force_build == 'true' }}
      set_latest: ${{ needs.detect-version.outputs.set_latest == 'true' }}
    secrets: inherit

  ip-bundle:
    needs: detect-version
    uses: ./.github/workflows/docker-image-nginx-ip-bundle.yml
    with:
      nginx_version: ${{ needs.detect-version.outputs.target_version }}
      force_build: ${{ github.event.inputs.force_build == 'true' }}
      set_latest: ${{ needs.detect-version.outputs.set_latest == 'true' }}
    secrets: inherit
